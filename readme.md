### TODO App (using Flask, HTMX and Alpine.js)

---

Before start run below commands

```sh
pip install -r requirements.txt
```
> Note: use python v3.10



To run in dev env run below command

```sh
flask run
```
or
```sh
export FLASK_DEBUG=1; flask run
```